#include <bits/stdc++.h>

using namespace std;

int N, M, T;
vector<int> A, B, C, E;

int main() {


    cin >> N >> M >> T;
    A.resize(M);
    B.resize(M);
    C.resize(M);
    E.resize(M);
    for (int i=0; i<M; i++)
        cin >> A[i] >> B[i] >> C[i] >> E[i];
    
    return 0;
}
